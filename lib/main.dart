import 'package:flutter/material.dart';

void main() {
  runApp(FileMan());
}

class FileMan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Container(
          child: Text("Coming soon"),
        ),
      ),
    );
  }
}
